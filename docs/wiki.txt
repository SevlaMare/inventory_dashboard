guidelines
  name the if conditions (use variables).
  move the code inside the ifs to HANDLERs.
  paid attention to TYPING.
  don't hard-coded array INDEXEs.
  destructured and named them appropriately.

--------------------------
CI -github actions

NOW
- dev mode: mock api VS intercept api calls?

--------------------------
IMPLEMENT
flow v1:
  # VIEW table with mocked api
  # implement user and admin
  # crud inventory = table (crud controls)
  # build up (filters etc)
  # design

--------------------------
FEATURES
rollback
multi-lingual (i18n)
accessibility (a11y)
observability (o11y)
RTK for queries (sync api + cache?)
PWA -> manifest + SW + assets list
------------
redux sync w/ local storage > problem with race condition.
  when autosave on.